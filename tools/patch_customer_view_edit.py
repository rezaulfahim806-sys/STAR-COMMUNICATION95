from pathlib import Path
import re

p=Path('app/src/main/assets/index.html')
s=p.read_text(encoding='utf-8')

# Replace the customer card with compact View + Edit actions while keeping existing payment/contact actions.
card_re=r"function card\(c\)\{.*?\}function customers\(\)\{"
new_card="""function card(c){let dv=due(c),pp=paid(c),run=Math.max(0,Number(c.fee||0)-Math.min(Number(c.fee||0),pp)),num=esc(c.phone||'');return `<div class=\"customer\"><div class=\"cmain\"><div class=\"avatar\">${esc((c.name||'?')[0].toUpperCase())}</div><div class=\"grow\"><div class=\"name\">${esc(c.name)}</div><div class=\"muted\">${esc(c.clientCode||'')} • ${num} • ${esc(c.pkg||'')}</div></div><span class=\"badge ${c.status}\">${String(c.status).toUpperCase()}</span></div><div class=\"actions\">${billBadge(c)}</div><div class=\"detail\"><span>Monthly Bill</span><b>${money(c.fee)}</b></div><div class=\"detail\"><span>Total Due</span><b>${money(dv)}</b></div><div class=\"actions\"><button class=\"small view\" onclick=\"go('details',{id:'${c.id}'})\">👁 View</button><button class=\"small btn light\" onclick=\"editCustomer('${c.id}')\">✏️ Edit</button><button class=\"small call\" onclick=\"callCustomer('${num}')\">📞 Call</button><button class=\"small wa\" onclick=\"waCustomer('${num}')\">WhatsApp</button>${dv>0?`<button class=\"small pay\" onclick=\"payCustomer('${c.id}')\">Pay</button>`:''}</div></div>`}function customers(){"""
s,n=re.subn(card_re,new_card,s,count=1,flags=re.S)
print('Replaced card:',n)

# Add a complete edit form before payment function.
marker='function payCustomer(id){'
edit_code="""function editCustomer(id){let c=d.customers.find(x=>String(x.id)===String(id));if(!c){toast('Customer not found');return}openSheet(`<h3>✏️ Edit Customer</h3><div class=\"muted\">Client Code</div><input id=\"encc\" class=\"input\" value=\"${esc(c.clientCode||'')}\" placeholder=\"Client Code\"><input id=\"enn\" class=\"input\" value=\"${esc(c.name||'')}\" placeholder=\"Customer name\"><input id=\"enp\" class=\"input\" value=\"${esc(c.phone||'')}\" placeholder=\"Mobile / WhatsApp\"><input id=\"ena\" class=\"input\" value=\"${esc(c.address||'')}\" placeholder=\"Address\"><input id=\"enpkg\" class=\"input\" value=\"${esc(c.pkg||'')}\" placeholder=\"Package / Mbps\"><input id=\"enfee\" class=\"input\" type=\"number\" value=\"${Number(c.fee||0)}\" placeholder=\"Monthly fee\"><input id=\"enpp\" class=\"input\" value=\"${esc(c.pppoeUser||c.pppoe||'')}\" placeholder=\"PPPoE username\"><input id=\"enpw\" class=\"input\" value=\"${esc(c.pppoePass||c.pppoePassword||'')}\" placeholder=\"PPPoE password\"><input id=\"enonu\" class=\"input\" value=\"${esc(c.onu||'')}\" placeholder=\"ONU ID\"><label class=\"muted\">Connection Date</label><input id=\"encd\" class=\"input\" type=\"date\" value=\"${esc(c.connectionDate||'')}\"><label class=\"muted\">Expiry Date</label><input id=\"enex\" class=\"input\" type=\"date\" value=\"${esc(c.expiry||'')}\"><input id=\"enprev\" class=\"input\" type=\"number\" value=\"${Number(c.prevDue||0)}\" placeholder=\"Previous due\"><select id=\"enst\" class=\"select\"><option value=\"active\" ${c.status==='active'?'selected':''}>Active</option><option value=\"inactive\" ${c.status==='inactive'?'selected':''}>Inactive</option><option value=\"expired\" ${c.status==='expired'?'selected':''}>Expired</option></select><button class=\"btn green full\" onclick=\"saveCustomerEdit('${c.id}')\">Save Changes</button><button class=\"btn light full\" style=\"margin-top:7px\" onclick=\"closeSheet()\">Cancel</button>`)}function saveCustomerEdit(id){let c=d.customers.find(x=>String(x.id)===String(id));if(!c)return;let code=(document.getElementById('encc')?.value||'').trim().toUpperCase(),name=(document.getElementById('enn')?.value||'').trim(),phone=(document.getElementById('enp')?.value||'').trim();if(!name||!phone){toast('Name and mobile required');return}if(!code)code=c.clientCode||('SC'+String(d.customers.indexOf(c)+1).padStart(3,'0'));let dup=d.customers.some(x=>String(x.id)!==String(c.id)&&String(x.clientCode||'').toUpperCase()===code);if(dup){toast('Client Code already exists');return}c.clientCode=code;c.name=name;c.phone=phone;c.address=(document.getElementById('ena')?.value||'').trim();c.pkg=(document.getElementById('enpkg')?.value||'').trim();c.fee=Number(document.getElementById('enfee')?.value||0);c.pppoeUser=(document.getElementById('enpp')?.value||'').trim();c.pppoePass=document.getElementById('enpw')?.value||'';c.onu=(document.getElementById('enonu')?.value||'').trim();c.connectionDate=document.getElementById('encd')?.value||c.connectionDate||today();c.expiry=document.getElementById('enex')?.value||'';c.prevDue=Number(document.getElementById('enprev')?.value||0);c.status=document.getElementById('enst')?.value||'active';if(c.expiry&&c.expiry<today())c.status='expired';save();closeSheet();toast('Customer updated');render()}"""
if marker in s and 'function editCustomer(id)' not in s:
    s=s.replace(marker,edit_code+marker,1)
    print('Added edit functions')
else:
    print('Edit functions already present or marker missing')

# Replace details page with a complete read-only customer profile, including PPPoE/ONU and client code.
details_re=r"function details\(\)\{.*?\}function callCustomer\(n\)\{"
new_details="""function details(){let c=d.customers.find(x=>String(x.id)===String(opts.id));if(!c){go('customers');return}let hist=d.payments.filter(x=>String(x.customer)===String(c.id)).sort((a,b)=>String(b.date).localeCompare(String(a.date)));return `<h1 class=\"title\">Customer Details</h1><div class=\"actions\"><button class=\"small view\" onclick=\"appBack()\">← Back</button><button class=\"small btn light\" onclick=\"editCustomer('${c.id}')\">✏️ Edit Customer</button></div><div class=\"customer\"><div class=\"cmain\"><div class=\"avatar\">${esc((c.name||'?')[0].toUpperCase())}</div><div class=\"grow\"><div class=\"name\">${esc(c.name)}</div><div class=\"muted\">Client Code: ${esc(c.clientCode||'—')} • ${esc(c.phone)}</div></div><span class=\"badge ${c.status}\">${String(c.status).toUpperCase()}</span></div><div class=\"detail\"><span>Client Code</span><b>${esc(c.clientCode||'—')}</b></div><div class=\"detail\"><span>Mobile / WhatsApp</span><b>${esc(c.phone||'—')}</b></div><div class=\"detail\"><span>Address</span><b>${esc(c.address||'—')}</b></div><div class=\"detail\"><span>Package / Mbps</span><b>${esc(c.pkg||'—')}</b></div><div class=\"detail\"><span>Monthly Bill</span><b>${money(c.fee)}</b></div><div class=\"detail\"><span>PPPoE Username</span><b>${esc(c.pppoeUser||c.pppoe||'—')}</b></div><div class=\"detail\"><span>PPPoE Password</span><b>${esc(c.pppoePass||c.pppoePassword||'—')}</b></div><div class=\"detail\"><span>ONU ID</span><b>${esc(c.onu||'—')}</b></div><div class=\"detail\"><span>Connection Date</span><b>${esc(c.connectionDate||'—')}</b></div><div class=\"detail\"><span>Expiry Date</span><b>${esc(c.expiry||'—')}</b></div><div class=\"detail\"><span>Previous Due</span><b>${money(c.prevDue)}</b></div><div class=\"detail\"><span>Running Bill</span><b>${money(billDue(c))}</b></div><div class=\"detail\"><span>Total Due</span><b>${money(due(c))}</b></div><div class=\"actions\"><button class=\"small call\" onclick=\"callCustomer('${esc(c.phone)}')\">📞 Call</button><button class=\"small wa\" onclick=\"waCustomer('${esc(c.phone)}')\">WhatsApp</button>${due(c)>0?`<button class=\"small pay\" onclick=\"payCustomer('${c.id}')\">Customer Bill Payment</button>`:''}</div></div><div class=\"section\"><h3>Payment History</h3>${hist.length?hist.map(x=>`<div class=\"detail\"><span>${esc(x.date)} • ${esc(x.month||'')} • ${esc(x.source||'manual')}</span><b>${money(x.amount)}</b></div>`).join(''):'<div class=\"muted\">No payments yet.</div>'}</div>`}function callCustomer(n){"""
s,n2=re.subn(details_re,new_details,s,count=1,flags=re.S)
print('Replaced details:',n2)

p.write_text(s,encoding='utf-8')
"